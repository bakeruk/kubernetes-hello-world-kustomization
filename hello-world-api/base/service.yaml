apiVersion: v1
kind: Service
metadata:
  name: hello-world-api-service
  labels:
    app: hello-world-api
    tier: backend
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80